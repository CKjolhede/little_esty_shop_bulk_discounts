<div class="nav-container">
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <p class="navbar-text"><%= @merchant.name %></p>
    <ul class="nav navbar-nav">
      <li><%= link_to 'Dashboard', merchant_dashboard_index_path, class: 'link_1', style: 'pull-right' %>
      <li><%= link_to 'Items', merchant_items_path, class: 'link_1', style: 'pull-right' %>
      <li><%= link_to 'Invoices', merchant_invoices_path, class: 'link_1', style: 'pull-right' %>
    </ul>
  </nav>
</div>
<body>
<h2>Update Bulk Discount Information</h2>
<%# <%= render partial: 'discount_form', locals: { method: :patch, url: "/merchants/#{@merchant.id}/bulk_discounts/#{@bulk_discount.id}", class: 'bulk_discount_form', local: true, model: @bulk_discount} %>
<%#percent: :value => @bulk_discount.percent, threshold: :value => @bulk_discounts.threshold,   %>

<%= form_with  url: "/merchant/#{@merchant.id}/bulk_discounts/#{@bulk_discount.id}", method: :patch, local: true do |form| %>
  <%= form.label :percent %>
  <%= form.number_field :percent, value: @bulk_discount.percent, in: 1..100, :step => 1 %><br/>
  <%= form.label :threshold %>
  <%= form.number_field :threshold, value: @bulk_discount.threshold, :step => 1 %><br/>
  <%= form.hidden_field :merchant_id, value: @merchant.id %><br/><br/>
  <%= form.submit 'Submit'%>
<% end %>